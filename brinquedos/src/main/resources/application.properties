# Nome da aplicação
spring.application.name=brinquedos

# Dados de conexão com o banco Oracle
spring.datasource.url=jdbc:oracle:thin:@oracle.fiap.com.br:1521:ORCL
spring.datasource.username=RM558935
spring.datasource.password=041195

spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# Dialeto do Oracle para Hibernate
spring.jpa.database-platform=org.hibernate.dialect.Oracle12cDialect

# Criação automática da tabela (DDL)
spring.jpa.hibernate.ddl-auto=update

# Exibir os SQLs gerados
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Corrige problemas de charset (opcional)
spring.datasource.hikari.connection-init-sql=ALTER SESSION SET NLS_LANGUAGE='AMERICAN'
